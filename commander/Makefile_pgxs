MODULE_big = commander

EXTENSION = commander
DATA = commander--1.0.sql

SHLIB_LINK += -lstdc++
PG_CPPFLAGS += -std=c++11
OBJS += command/command.o

PG_CONFIG = pg_config
PGXS := $(shell $(PG_CONFIG) --pgxs)
include $(PGXS)
